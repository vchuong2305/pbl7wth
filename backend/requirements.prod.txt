# Production/server dependencies for Railway (Linux)
# Keep this lean to avoid Windows-only deps (e.g. tensorflow-intel, pywin32)

fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.6

# Data / ML runtime
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
tensorflow==2.13.0
h5py==3.13.0

# DB + migrations (SQLite by default; can be overridden via env vars if you refactor)
sqlalchemy==2.0.27
alembic==1.12.1

# Settings / misc
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1
requests==2.31.0
apscheduler==3.10.4
schedule==1.2.2
